/**
 * SilentPassUI — vertical rhythm under VerraFloatingNavChrome (same as Create your Verra ID).
 * Tailwind JIT: keep literals here so classes are picked up from this file (see tailwind content).
 */
export const APP_FLOATING_CHROME_MAIN_TOP_PT =
	'pt-[calc(env(safe-area-inset-top)+3.75rem)]' as const

/** Margin below the page title + subtitle block before the first control (QR / form field). */
export const APP_TITLE_BLOCK_TO_FIRST_CONTROL_MB = 'mb-12' as const
